import 'package:flutter/material.dart';

/// Brand colors aligned with the Dayla web app (Tailwind / index.html).
abstract final class AppColors {
  /// PWA theme-color / primary forest green
  static const Color primary = Color(0xFF3A5A40);

  /// Page background (#f7f3ee)
  static const Color background = Color(0xFFF7F3EE);

  /// Scrollbar / accent sage
  static const Color sage = Color(0xFFA3B18A);

  /// Warm accent used in dashboard cards
  static const Color sand = Color(0xFFD4A373);
}
